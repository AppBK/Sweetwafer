#1. using the up and down arrows of an input for different tasks:
  Key in using this React specific syntax: event.nativeEvent.offsetY (or offsetX);


#2. transition: background-color 0.25s ease; // Use ease to have a change go smoothly in and out


#3. add commas to number: number.toLocaleString([locale [, options]]);











####### POSSIBLY #############

1. height: calc(100vh - 16px); // can this fix issue with fixed position footer?!!
